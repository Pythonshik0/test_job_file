# Агент сбора информации об ОС Windows
# Используется только стандартная библиотека Python 3 (configparser, logging, queue, threading, winreg и т.д.).
# Для работы в venv достаточно: python -m venv venv && venv\Scripts\activate
